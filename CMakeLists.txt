SET(LIB_SRC unity.c)
ADD_LIBRARY(libunity SHARED ${LIB_SRC})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
SET_TARGET_PROPERTIES(libunity PROPERTIES OUTPUT_NAME unity)
INSTALL(TARGETS libunity
      LIBRARY DESTINATION lib
      ARCHIVE DESTINATION lib)
